package br.com.conectabyte.knowly.identity;

/** What a {@code profile_edit_request_contacts} row proposes doing to a {@link Contact} row. */
public enum ContactChangeAction {
    ADD,
    UPDATE,
    REMOVE
}
