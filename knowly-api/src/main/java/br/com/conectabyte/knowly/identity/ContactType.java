package br.com.conectabyte.knowly.identity;

/** Closed set of reachability channels a {@link Contact} row can represent (REQ-3). */
public enum ContactType {
    PHONE,
    WHATSAPP,
    EMAIL,
    OTHER
}
