# knowly

Monorepo for the knowly product: internal knowledge base with an AI layer.

- [`knowly-api/`](knowly-api/) — Spring Boot API (formerly the `knowly` repo)
- [`knowly-app/`](knowly-app/) — Angular app (formerly the `knowly-app` repo)

See `VISION.md`, `PROJECT_STATUS.md`, `DECISIONS.md` at the repo root for
the product-level docs that used to be duplicated/pointed-to across two
repos.

