## Summary

<!-- What does this PR do and why? -->

## SPEC / requirement addressed

<!-- Link to the SPEC.md this PR implements, e.g. specify/features/tags-list/SPEC.md -->
<!-- Which REQ-N(s) from that SPEC does this PR satisfy? -->

## Checklist

- [ ] SPEC/PLAN/TASKS created or updated for this change (if behavior
      changed)
- [ ] Tests written before implementation (TDAD) and passing
- [ ] `npm run format` run — no formatting diff
- [ ] `npm run format:check && npm test && npm run build` passes locally
- [ ] `constitution.md`/`CLAUDE.md` updated if a new convention was
      introduced
