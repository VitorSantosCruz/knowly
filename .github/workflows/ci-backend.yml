name: CI (backend)

on:
  push:
    branches: [main]
    paths:
      - 'knowly-api/**'
      - '.github/workflows/ci-backend.yml'
  pull_request:
    paths:
      - 'knowly-api/**'
      - '.github/workflows/ci-backend.yml'

defaults:
  run:
    working-directory: knowly-api

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up JDK
        uses: actions/setup-java@v5.6.0
        with:
          distribution: 'temurin'
          java-version-file: 'knowly-api/.java-version'
          cache: 'maven'
          cache-dependency-path: 'knowly-api/pom.xml'

      - name: Install Tesseract OCR
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends tesseract-ocr tesseract-ocr-eng tesseract-ocr-por

      - name: Check formatting (Spotless)
        run: ./mvnw -B spotless:check

      - name: Run tests
        run: ./mvnw -B verify

      - name: Build Docker image
        run: docker build -t knowly-backend:ci .

  publish:
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract image metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{ github.repository }}-backend
          tags: |
            type=sha
            type=raw,value=latest

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: knowly-api
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
