> **Reference example** — see notice in SPEC.md.

# TASKS — Tags management screen

- [ ] 1. Write `tags.service.spec.ts` covering REQ-1, REQ-2, REQ-3 (Red).
- [ ] 2. Implement `TagsService` (`list`, `create`, `remove`) until task 1's
      tests pass (Green).
- [ ] 3. Write `tags-page.component.spec.ts` covering REQ-1 through REQ-5
      (Red).
- [ ] 4. Implement `TagsPageComponent` (template + logic) until task 3's
      tests pass (Green).
- [ ] 5. Register the `/tags` route in `app.routes.ts`.
- [ ] 6. Run `npm run format:check && npm test && npm run build` and
      confirm everything is green.
- [ ] 7. Update `PLAN.md` if any decision changed during implementation.
